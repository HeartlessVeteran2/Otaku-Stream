<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required by the Wi-Fi-only check: without it ConnectivityManager throws at runtime. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- dataSync, not mediaPlayback: the player already runs its own mediaPlayback service, and this
         one is about moving bytes, which continues while playback is paused. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 13+ gates the notification behind a runtime grant. The download and the service run
         either way; without the grant the user just can't see or stop it from the shade. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name="com.otakustream.core.torrent.TorrentService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
